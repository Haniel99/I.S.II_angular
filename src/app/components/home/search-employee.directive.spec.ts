import { SearchEmployeeDirective } from './search-employee.directive';

describe('SearchEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
